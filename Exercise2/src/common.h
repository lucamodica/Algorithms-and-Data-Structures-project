/* This header was created to include all common libreries between 
edit_distance, words_list_manager, the main and the tests file, to
avoid conflicts */ 
#ifndef COMMON
#define COMMON

#include <stdlib.h>
#include <stdio.h>
#include <string.h>

#endif